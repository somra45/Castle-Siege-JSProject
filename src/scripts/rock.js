export default class Rock {
    constructor(rockObject, ctx) {
        this.height = rockObject.height;
        this.width = rockObject.width;
        this.velocity = rockObject.velocity;
        this.acceleration = rockObject.acceleration;
        this.ctx = ctx;
    };

    reset(ctx) {
        
    };

    mousepos(ctx) {

    };

    
}